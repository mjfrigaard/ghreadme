% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_gif.R
\name{make_gif}
\alias{make_gif}
\title{Assemble a directory of PNG frames into a GIF}
\usage{
make_gif(
  frame_dir,
  output,
  width,
  height,
  first_duration = 1,
  frame_duration = 0.4,
  last_duration = 4
)
}
\arguments{
\item{frame_dir}{Path to a directory containing numbered \code{.png} frame files.}

\item{output}{Path for the output \code{.gif} file (e.g. \code{"animation.gif"}).}

\item{width}{GIF width in pixels.}

\item{height}{GIF height in pixels.}

\item{first_duration}{Seconds to hold the first frame (default \code{1}).}

\item{frame_duration}{Seconds to show each middle frame (default \code{0.4}).}

\item{last_duration}{Seconds to hold the last frame (default \code{4}).}
}
\value{
Invisibly returns \code{output}.
}
\description{
Collects every \code{.png} file in \code{frame_dir} (sorted alphabetically), builds
a per-frame delay vector with distinct hold times for the first and last
frames, and writes the result to a GIF via \code{\link[gifski:gifski]{gifski::gifski()}}.
}
\details{
The typical workflow is:
\enumerate{
\item Create a temporary directory.
\item Loop over your data, saving one plot per frame with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
\item Call \code{make_gif()} to assemble the frames.
}
}
\examples{
\dontrun{
frame_dir <- file.path(tempdir(), "my-frames")
dir.create(frame_dir, showWarnings = FALSE)

for (i in 1:5) {
  p <- ggplot2::ggplot() + ggplot2::ggtitle(paste("Frame", i))
  ggplot2::ggsave(
    filename = file.path(frame_dir, sprintf("\%04d.png", i)),
    plot     = p,
    width    = 800,
    height   = 400,
    units    = "px"
  )
}

make_gif(frame_dir, "animation.gif", width = 800, height = 400)
}
}
